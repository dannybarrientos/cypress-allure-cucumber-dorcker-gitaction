version: "3"
services:
  # this container will run Cypress test using built-in Electron browser
  e2e:
    image: "uat-test:1.0"
    command: "cypress:execution-tags"
    volumes:
      - ./cypress/reports:/uat-test/cypress/reports